{
  "name": "xyz_backend",
  "version": "1.0.0",
  "description": "Some good description",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "NODE_ENV=production node app.js",
    "build": "tsc",
    "predev": "npm run build",
    "dev": "NODE_ENV=development nodemon -q build/app.js"
  },
  "author": "Joe Doe",
  "license": "ISC",
  "devDependencies": {
    "@types/cors": "*",
    "@types/express": "*",
    "@types/node": "*",
    "nodemon": "*",
    "typescript": "*"
  },
  "dependencies": {
    "axios": "*",
    "cors": "*",
    "dotenv": "*",
    "express": "*",
    "winston": "^*",
    "winston-transport": "*"
  }
}
